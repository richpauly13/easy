import { WidthDirective } from './width.directive';

describe('WidthDirective', () => {
	it('should create an instance', () => {
		const directive: WidthDirective = new WidthDirective();

		expect(directive).toBeTruthy();
	});
});
