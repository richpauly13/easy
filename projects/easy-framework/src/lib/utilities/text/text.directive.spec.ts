import { TextDirective } from './text.directive';

describe('TextDirective', () => {
	it('should create an instance', () => {
		const directive: TextDirective = new TextDirective();

		expect(directive).toBeTruthy();
	});
});
