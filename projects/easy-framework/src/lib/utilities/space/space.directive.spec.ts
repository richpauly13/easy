import { SpaceDirective } from './space.directive';

describe('SpaceDirective', () => {
    it('should create an instance', () => {
        const directive = new SpaceDirective();
        
        expect(directive).toBeTruthy();
    });
});
