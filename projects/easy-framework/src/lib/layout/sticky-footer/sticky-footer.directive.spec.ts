import { StickyFooterDirective } from './sticky-footer.directive';

describe('StickyFooterDirective', () => {
	it('should be created', () => {
		const directive: StickyFooterDirective = new StickyFooterDirective();

		expect(directive).toBeTruthy();
	});
});
