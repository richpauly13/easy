import { MultiColumnDirective } from './multi-column.directive';

describe('MultiColumnDirective', () => {
	it('should be created', () => {
		const directive: MultiColumnDirective = new MultiColumnDirective();

		expect(directive).toBeTruthy();
	});
});
